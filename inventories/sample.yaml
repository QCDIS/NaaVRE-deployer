all:
  vars:
    ansible_ssh_user: user
    internal_network_interface: ensX
    pod_network_cidr: '10.244.0.0/16'

  children:

    master:
      hosts:
        host1:

    workers:
      hosts:
        host2:
        host3:
